# VPN gateway used by the TLS relay.
VPN_GATEWAY_HOST=vpn.example.org
VPN_GATEWAY_PORT=44443

# Host and port reached by the fixed SSH forward after VPN login.
SSH_TARGET=VPN_TARGET_IP
SSH_TARGET_PORT=22
